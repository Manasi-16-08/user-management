import { addNumbers, getCurrentDate } from './index.js';

// Test adding two numbers
console.log(addNumbers(5, 10)); // Expected output: 15

// Test getting current date
console.log(getCurrentDate()); // Expected output: Current date in "Year/Month/Day" format

// Call other functions for testing if needed.
fetchAllPosts();
fetchPostById(5);
createUser();
deleteUser(2);
fetchUserTodos();
